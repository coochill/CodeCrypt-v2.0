Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
python-dotenv==1.0.0
# Explicit SQLAlchemy pin (Python 3.13 compatibility & stability)
SQLAlchemy>=2.0.30,<2.1
# Postgres driver (binary build for easy install); optional if staying on SQLite
# Updated to a Python 3.13 compatible release. If you are not using Postgres yet,
# you may comment this line out to skip installing the driver.
psycopg[binary]>=3.2.8,<3.3
# Alembic for migrations (future-proofing)
alembic==1.13.2
waitress==3.0.0